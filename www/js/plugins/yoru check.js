function macho() {
  machoAcount = $gameVariable.value(1);
  erogauge = $gameVariables.value(45);
  machoZyokyou = $gameVariable.value(41);
  leaderZyokyou = $gameVariable.value(42);
  succuZyokyou = $gameVariable.value(43);
  slimeZyokyou = $gameVariable.value(44);

  switch (machoAcount) {
    case 0:
      if (erogauge == 30 && machoZyokyou == 30) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(38, 11, 5, 2, 0);
      } else {
        자위;
      }
      break;
    case 1:
      if (erogauge == 45 && machoZyokyou == 45) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(54, 10, 15, 2, 0);
      } else {
        자위;
      }
      break;
    case 2:
      if (erogauge == 45 && machoZyokyou == 45) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(82, 11, 3, 2, 0);
      } else {
        자위;
      }
      break;
    case 3:
      if (erogauge == 45 && machoZyokyou == 45) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(56, 12, 1, 2, 0);
      } else {
        자위;
      }
      break;
    case 4:
      if (erogauge == 45 && machoZyokyou == 45) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(57, 1, 0, 2, 0);
      } else {
        자위;
      }
      break;
    case 5:
      if (erogauge == 45 && machoZyokyou == 45) {
        this._params = [799];
        this.command117();
        $gamePlayer.reserveTransfer(58, 1, 0, 2, 0);
      } else {
        자위;
      }
      break;

    default:
      undefined;
  }
}

switch (leaderZyokyou) {
  case 0:
    if (erogauge == 30 && machoZyokyou == 30) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(47, X, Y, 2, 0);
    } else {
      자위;
    }
    break;
  case 1:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(59, X, Y, 2, 0);
    } else {
      자위;
    }
    break;
  case 2:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(61, 6, 0, 2, 0);
    } else {
      자위;
    }
    break;
  case 3:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(207, X, Y, 2, 0);
    } else {
      자위;
    }
    break;
  case 4:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(63, 10, 11, 2, 0);
    } else {
      자위;
    }
    break;
  case 5:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(208, 13, 15, 2, 0);
    } else {
      자위;
    }
    break;

  default:
    undefined;
}

switch (succuZyokyou) {
  case 0:
    if (erogauge == 30 && machoZyokyou == 30) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(66, 16, 1, 2, 0);
    } else {
      자위;
    }
    break;
  case 1:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(67, 1, 0, 2, 0);
    } else {
      자위;
    }
    break;
  case 2:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(187, 1, 0, 2, 0);
    } else {
      자위;
    }
    break;
  case 3:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(69, 5, 6, 2, 0);
    } else {
      자위;
    }
    break;
  case 4:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(70, 10, 20, 2, 0);
    } else {
      자위;
    }
    break;
  case 5:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(73, 3, 7, 2, 0);
    } else {
      자위;
    }
    break;

  default:
    undefined;
}

switch (slimeZyokyou) {
  case 0:
    if (erogauge == 30 && machoZyokyou == 30) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(76, 3, 2, 2, 0);
    } else {
      자위;
    }
    break;
  case 1:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(75, 22, 5, 2, 0);
    } else {
      자위;
    }
    break;
  case 2:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(77, 6, 4, 2, 0);
    } else {
      자위;
    }
    break;
  case 3:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(131, 3, 1, 2, 0);
    } else {
      자위;
    }
    break;
  case 4:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(79, 4, 1, 2, 0);
    } else {
      자위;
    }
    break;
  case 5:
    if (erogauge == 45 && machoZyokyou == 45) {
      this._params = [799];
      this.command117();
      $gamePlayer.reserveTransfer(83, 8, 5, 2, 0);
    } else {
      자위;
    }
    break;

  default:
    undefined;
}
